# test 의 apllication.yml
# spring.io -> Projects -> Spring Boot -> Learn -> spring ??? ?? reference ??
spring:
#  datasource:
#    url : jdbc:h2:mem:test  # in-memory , 참고로 Spring Boot는 별도의 설정이 없으면 메모리 모드로 실행한다.
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver
#
#  jpa:
#    hibernate:
#      ddl-auto: create  # create 는 기존테이블 삭제 후 다시 생성 (DROP + CREATE)
#    properties:
#      hibernate:
#        #show_sql: true  # ??? ??? ??? ??
#        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type: trace # 쿼리 파라미터 로그 남기기